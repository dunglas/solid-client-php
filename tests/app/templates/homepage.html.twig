{% extends 'base.html.twig' %}

{% block body %}
    <h1>Solid Demo App</h1>
    {% if app.user %}
        <a href="{{ logout_path() }}">Logout</a>
    {% else %}
        <a href="{{ path('solid_client_login')}}">Login</a>
    {% endif %}
{% endblock %}
